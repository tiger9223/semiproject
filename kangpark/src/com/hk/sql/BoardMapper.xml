<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hk.board">

	<sql id="a">
		SELECT SEQ,ID,TITLE,CONTENT,REGDATE,REFER,STEP,DEPTH,READCOUNT,DELFLAG 
		FROM BOARD 
	</sql>

	<select id="boardlist" resultType="BoardDto" parameterType="Map">
		<include refid="a"/>
		<where>
			<if test="seq!=null">
				SEQ=#{seq}					
			</if>
		</where>
		ORDER BY REFER,STEP 
	</select>
	
	<select id="getboard" parameterType="int">
		<include refid="a"/>
		WHERE SEQ=#{seq}
	</select>
	
	<insert id="insertboard" parameterType="BoardDto">
		INSERT INTO BOARD (SEQ,ID,TITLE,CONTENT,REGDATE,REFER,
		STEP,DEPTH,READCOUNT,DELFLAG) 
		VALUES(BOARD_SEQ.NEXTVAL,#{id},#{title},#{content},SYSDATE, 
		(SELECT NVL(MAX(REFER)+1,0) FROM BOARD ),0,0,0,'N')
	</insert>
	
	<update id="updateboard" parameterType="BoardDto">
		UPDATE BOARD SET TITLE=#{title},CONTENT=#{content},REGDATE=SYSDATE 
		WHERE SEQ=#{seq} 
	</update>
	
	<update id="muldel" parameterType="Map">
		UPDATE BOARD SET DELFLAG='Y' WHERE SEQ IN 
		<foreach collection="seqs" item="seq" open="(" close=")" separator=",">
			#{seq}
		</foreach>
	</update>
	
	<update id="readcount" parameterType="int">
		<![CDATA[
		UPDATE BOARD SET READCOUNT=READCOUNT+1
		WHERE SEQ=#{seq} 
		]]>
	</update>
	
	<update id="replyupdate" parameterType="BoardDto">
		<![CDATA[
			UPDATE BOARD SET STEP=STEP+1
			WHERE REFER=(SELECT REFER FROM BOARD WHERE SEQ=#{seq})
			AND STEP > (SELECT STEP FROM BOARD WHERE SEQ=#{seq})		
		]]>
	</update>
	
	<insert id="replyinsert" parameterType="BoardDto">
		INSERT INTO BOARD 
		VALUES(BOARD_SEQ.NEXTVAL,#{id},#{title},#{content},SYSDATE, 
		(SELECT REFER FROM BOARD WHERE SEQ=#{seq}), 
		(SELECT STEP FROM BOARD WHERE SEQ=#{seq})+1, 
		(SELECT DEPTH FROM BOARD WHERE SEQ=#{seq})+1,0,'N')
	</insert>
	
	<select id="contentAjax" parameterType="int" resultType="BoardDto">
		SELECT CONTENT FROM BOARD WHERE SEQ=#{seq}
	</select>
</mapper>